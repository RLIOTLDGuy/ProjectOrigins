if place_meeting(x, y, par_player)
	instance_destroy();
	
grav = 0.6;
		if (grounded) && usesGravity = true
			hsp = approach(hsp, 0, 0.3);
		scr_collision();
		
		if usesGravity = false
			grav = 0;
			hsp = 0;